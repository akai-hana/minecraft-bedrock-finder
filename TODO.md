# Bugs
- When i'm in fullscreen, i can't see the entirety of the UI (the bottom box). Maybe add some zoom option, or improve the automatic zoom of the program to automatically adapt to the dimensions of the window

# UI
- Add click-and-drag horizontal bars to the grid dimensions

---

- add an option to switch between the two searches as a compatibility fallback for the users that are stuck un webgpu/metal/dx12 backends and cannot user vulkan for whatever reason? i want to see a checkbox on the GUI, and then implement whatever logic is necessary on the backend.

---

(what is the offset for?)
